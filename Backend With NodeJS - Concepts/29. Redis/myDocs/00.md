
# ✅ REDIS — MUST KNOW COMMANDS (CLI LEVEL)

## 1️⃣ Enter Redis

```bash
docker exec -it redis redis-cli
```

---

## 2️⃣ Exit Redis

(only Redis CLI commands work here)

```bash
exit
# or
quit
# or
Ctrl + D
```

---

## 3️⃣ Check Redis is working

```redis
PING
```

Expected:

```
PONG
```

---

## 4️⃣ Basic Redis data commands

### STRING

```redis
SET name "Tanish"
GET name
DEL name
```

---

### LIST

```redis
LPUSH user:1:logins "2026-01-09 10:30"
RPUSH user:1:logins "2026-01-09 09:00"
LRANGE user:1:logins 0 -1
```

---

### SET

```redis
SADD user:1:devices "chrome"
SADD user:1:devices "iphone"
SMEMBERS user:1:devices
```

---

### HASH

```redis
HSET user:1:profile name "Tanish" city "Delhi"
HGET user:1:profile name
HGETALL user:1:profile
```

---

### ZSET

```redis
ZADD leaderboard 150 tanish
ZADD leaderboard 200 rohit
ZRANGE leaderboard 0 -1 WITHSCORES
```

---

## 5️⃣ Inspect Redis data

```redis
KEYS *
TYPE leaderboard
```

---

---

# 🐳 DOCKER — MUST KNOW COMMANDS (SYSTEM LEVEL)

These run **ONLY at**:

```
C:\Users\TK>
```

---

## 1️⃣ Check running containers

```bash
docker ps
```

---

## 2️⃣ Check all containers (running + stopped)

```bash
docker ps -a
```

---

## 3️⃣ Start Redis container

```bash
docker start redis
```

---

## 4️⃣ Stop Redis container

```bash
docker stop redis
```

---

## 5️⃣ Restart Redis

```bash
docker restart redis
```

---

## 6️⃣ Remove container (only if needed)

```bash
docker rm redis
```

---

## 7️⃣ Create Redis container (ONE TIME)

```bash
docker run -d -p 6379:6379 --name redis redis
```

---

## 8️⃣ See container logs

```bash
docker logs redis
```

---

---

# 🧠 ONE GOLDEN RULE (remember this forever)

| Prompt            | Allowed commands     |
| ----------------- | -------------------- |
| `127.0.0.1:6379>` | Redis commands only  |
| `C:\Users\TK>`    | Docker / OS commands |

If you follow this, **90% errors disappear**.

---
