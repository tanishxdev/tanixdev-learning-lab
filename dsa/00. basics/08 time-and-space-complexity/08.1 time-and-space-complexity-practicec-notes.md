## Time Complexity & Space Complexity – Practice (Basics)

This file is for **building intuition** on:

* Time Complexity (TC)
* Space Complexity (SC)
* Loop patterns
* Common interview tricks

👉 **Rule**:
First analyze yourself, then check solutions.

---

## PRACTICE QUESTIONS (20)

### Q1

```cpp
for(int i = 0; i < n; i++)
    cout << i;
```

---

### Q2

```cpp
int x = 10;
```

---

### Q3

```cpp
int arr[n];
```

---

### Q4

```cpp
for(int i = 0; i < n; i++)
    for(int j = 0; j < n; j++)
        cout << i + j;
```

---

### Q5

```cpp
for(int i = 1; i <= n; i = i * 2)
    cout << i;
```

---

### Q6

```cpp
for(int i = 0; i < n; i++)
    for(int j = 0; j < i; j++)
        cout << "*";
```

---

### Q7

```cpp
for(int i = 0; i < n; i++)
    cout << "Hello";
cout << "World";
```

---

### Q8

```cpp
int sum = 0;
for(int i = 0; i < n; i++)
    sum += i;
```

---

### Q9

```cpp
for(int i = n; i > 0; i--)
    cout << i;
```

---

### Q10

```cpp
for(int i = 0; i < n; i++)
    for(int j = 1; j < n; j = j * 2)
        cout << i << j;
```

---

### Q11

```cpp
int a = 5;
int b = 10;
int c = a + b;
```

---

### Q12

```cpp
vector<int> v(n);
```

---

### Q13

```cpp
for(int i = 0; i < n; i++)
{
    if(i == 5)
        break;
}
```

---

### Q14

```cpp
for(int i = 0; i < n; i++)
{
    for(int j = 0; j < n; j++)
        for(int k = 0; k < n; k++)
            cout << i;
}
```

---

### Q15

```cpp
while(n > 0)
{
    n = n / 2;
}
```

---

### Q16

```cpp
int arr[1000];
```

---

### Q17

```cpp
for(int i = 0; i < n; i++)
{
    for(int j = 0; j < i; j++)
        cout << j;
}
```

---

### Q18

```cpp
int x = 0;
for(int i = 0; i < n; i++)
    x++;
```

---

### Q19

```cpp
for(int i = 0; i < n; i++)
    for(int j = 0; j < 5; j++)
        cout << i;
```

---

### Q20

```cpp
for(int i = 0; i < n; i++)
{
    cout << i;
}
int arr[n];
```

---

# SOLUTIONS (WITH THINKING PROCESS + DRY RUN)

---

## Q1 Solution

```cpp
for(int i = 0; i < n; i++)
    cout << i;
```

**TC**: O(N)
**SC**: O(1)

**Thinking**
One loop, runs based on `n`. No extra memory.

**Dry Run**

```
n = 5
i = 0 → print
i = 1 → print
i = 2 → print
i = 3 → print
i = 4 → print
Total operations ≈ n
```

---

## Q2 Solution

```cpp
int x = 10;
```

**TC**: O(1)
**SC**: O(1)

**Thinking**
Single assignment, independent of input size.

**Dry Run**

```
x assigned once
No loop, no growth
```

---

## Q3 Solution

```cpp
int arr[n];
```

**TC**: O(1)
**SC**: O(N)

**Thinking**
Array creation doesn’t loop, but memory depends on `n`.

**Dry Run**

```
n = 5 → arr[5]
n = 1000 → arr[1000]
Memory grows linearly
```

---

## Q4 Solution

```cpp
for(int i = 0; i < n; i++)
    for(int j = 0; j < n; j++)
        cout << i + j;
```

**TC**: O(N²)
**SC**: O(1)

**Thinking**
Nested loop → multiplication of iterations.

**Dry Run**

```
n = 3
i = 0 → j runs 3 times
i = 1 → j runs 3 times
i = 2 → j runs 3 times
Total = 3 × 3 = 9 operations
```

---

## Q5 Solution

```cpp
for(int i = 1; i <= n; i = i * 2)
    cout << i;
```

**TC**: O(log N)
**SC**: O(1)

**Thinking**
`i` doubles each step → exponential jump.

**Dry Run**

```
n = 16
i = 1
i = 2
i = 4
i = 8
i = 16
Steps = log₂(16) = 4
```

---

## Q6 Solution

```cpp
for(int i = 0; i < n; i++)
    for(int j = 0; j < i; j++)
        cout << "*";
```

**TC**: O(N²)
**SC**: O(1)

**Thinking**
Inner loop depends on `i`.

**Dry Run**

```
i=0 → 0 prints
i=1 → 1 print
i=2 → 2 prints
...
Total = 1 + 2 + ... + n ≈ n²/2
```

---

## Q7 Solution

```cpp
for(int i = 0; i < n; i++)
    cout << "Hello";
cout << "World";
```

**TC**: O(N)
**SC**: O(1)

**Thinking**
One loop + one constant statement.

**Dry Run**

```
n = 5
"Hello" printed 5 times
"World" printed once
Dominant term = n
```

---

## Q8 Solution

```cpp
int sum = 0;
for(int i = 0; i < n; i++)
    sum += i;
```

**TC**: O(N)
**SC**: O(1)

**Thinking**
Extra variables are constant.

**Dry Run**

```
n = 4
i=0 → sum=0
i=1 → sum=1
i=2 → sum=3
i=3 → sum=6
Loop runs n times
```

---

## Q9 Solution

```cpp
for(int i = n; i > 0; i--)
    cout << i;
```

**TC**: O(N)
**SC**: O(1)

**Thinking**
Reverse loop still runs `n` times.

**Dry Run**

```
n=5
i=5,4,3,2,1
Total iterations = n
```

---

## Q10 Solution

```cpp
for(int i = 0; i < n; i++)
    for(int j = 1; j < n; j = j * 2)
        cout << i << j;
```

**TC**: O(N log N)
**SC**: O(1)

**Thinking**
Outer loop linear, inner logarithmic.

**Dry Run**

```
n=8
Outer loop → 8 times
Inner j: 1,2,4 → log n steps
Total = n × log n
```

---

## Q11 Solution

```cpp
int a = 5;
int b = 10;
int c = a + b;
```

**TC**: O(1)
**SC**: O(1)

**Thinking**
Only constant operations.

**Dry Run**

```
3 assignments
Always same
```

---

## Q12 Solution

```cpp
vector<int> v(n);
```

**TC**: O(1)
**SC**: O(N)

**Thinking**
Memory depends on `n`.

**Dry Run**

```
n=5 → vector size 5
n=100 → vector size 100
```

---

## Q13 Solution

```cpp
for(int i = 0; i < n; i++)
{
    if(i == 5)
        break;
}
```

**TC**: O(N)
**SC**: O(1)

**Thinking**
Worst case → `n < 5` or break never hit.

**Dry Run**

```
Worst case: i never equals 5
Loop runs full n times
```

---

## Q14 Solution

```cpp
for(int i = 0; i < n; i++)
{
    for(int j = 0; j < n; j++)
        for(int k = 0; k < n; k++)
            cout << i;
}
```

**TC**: O(N³)
**SC**: O(1)

**Thinking**
Three nested loops → multiply.

**Dry Run**

```
n=3
3 × 3 × 3 = 27 operations
```

---

## Q15 Solution

```cpp
while(n > 0)
{
    n = n / 2;
}
```

**TC**: O(log N)
**SC**: O(1)

**Thinking**
`n` reduces by half.

**Dry Run**

```
n=16
16 → 8 → 4 → 2 → 1
Steps = log n
```

---

## Q16 Solution

```cpp
int arr[1000];
```

**TC**: O(1)
**SC**: O(1)

**Thinking**
Fixed size array.

**Dry Run**

```
Always allocates 1000 integers
```

---

## Q17 Solution

```cpp
for(int i = 0; i < n; i++)
{
    for(int j = 0; j < i; j++)
        cout << j;
}
```

**TC**: O(N²)
**SC**: O(1)

**Dry Run**

```
i=0 → 0
i=1 → 1
i=2 → 2
Total ≈ n²/2
```

---

## Q18 Solution

```cpp
int x = 0;
for(int i = 0; i < n; i++)
    x++;
```

**TC**: O(N)
**SC**: O(1)

**Dry Run**

```
x increments n times
```

---

## Q19 Solution

```cpp
for(int i = 0; i < n; i++)
    for(int j = 0; j < 5; j++)
        cout << i;
```

**TC**: O(N)
**SC**: O(1)

**Thinking**
Inner loop is constant.

**Dry Run**

```
n × 5 → ignore constant
```

---

## Q20 Solution

```cpp
for(int i = 0; i < n; i++)
{
    cout << i;
}
int arr[n];
```

**TC**: O(N)
**SC**: O(N)

**Dry Run**

```
Loop → n operations
Array → n memory
```

---

### FINAL MENTAL MODEL (REMEMBER THIS)

* **Loop → add**
* **Nested loop → multiply**
* **Divide / multiply variable → log**
* **Array / vector → space = size**
* **Ignore constants**
