console.log(1 === "1"); // Strict equality operator (===) checks for both value and type, so it returns false
console.log(1 == "1"); // Loose equality operator (==) performs type coercion, so it converts the string "1" to the number 1 and then compares, resulting in true
