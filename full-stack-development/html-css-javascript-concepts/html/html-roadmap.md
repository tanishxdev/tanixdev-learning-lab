# HTML SYLLABUS (SECTIONWISE ORGANIZED)

---

# SECTION 1 — HTML FOUNDATIONS

These topics build the base of HTML structure and syntax.

1. HTML Introduction
2. How HTML Works (Browser, DOM)
3. HTML Editors (VS Code setup)
4. HTML Basic Structure (`<!DOCTYPE>`, `<html>`, `<head>`, `<body>`)
5. HTML Elements (opening/closing tags)
6. HTML Attributes (global + element-specific)
7. HTML Headings (`<h1>–<h6>`)
8. HTML Paragraphs (`<p>`)
9. HTML Styles (inline styling)
10. HTML Formatting Tags (`<b>`, `<i>`, `<strong>`, `<em>`, etc.)
11. HTML Quotations (`<blockquote>`, `<q>`…)
12. HTML Comments
13. HTML Colors (inline, color names, hex, rgb)
14. HTML Entities & Symbols
15. HTML Emojis
16. HTML Charsets (UTF-8)
17. HTML URL Encoding
18. HTML vs XHTML

---

# SECTION 2 — HTML DOCUMENT STRUCTURE

Learn how a professional HTML page is organized.

1. `<head>` Tag Deep Dive
2. Title & Metadata (`<title>`, `<meta>`)
3. Favicon Setup
4. Linking CSS & JS (`<link>`, `<script>`)
5. File Paths (absolute, relative)
6. HTML Layout Basics (header, nav, main, aside, footer)
7. Block vs Inline Elements
8. `<div>` and structural grouping
9. Semantic HTML Overview (to be detailed later)

---

# SECTION 3 — HTML TEXT & CONTENT ELEMENTS

Used to build content inside web pages.

1. Headings
2. Paragraphs
3. Line Breaks & Horizontal Rules
4. Formatting Tags (bold, italic, etc.)
5. Quotations
6. Lists

   * Ordered
   * Unordered
   * Description lists
7. Tables (caption, thead, tbody, tfoot)
8. Links

   * Internal
   * External
   * Email links
   * Button links
   * Bookmark links
9. Images
10. Image Attributes (alt, height, width)
11. Background Images (via CSS reference)

---

# SECTION 4 — HTML MEDIA & GRAPHICS

Multimedia integration and graphics.

## Media

1. HTML Video
2. HTML Audio
3. HTML YouTube Embed
4. HTML Plug-ins (deprecated concepts)

## Graphics

1. HTML Canvas (with JS)
2. HTML SVG Graphics
3. Differences: Canvas vs SVG
4. Favicon Icons (PNG, ICO)

---

# SECTION 5 — HTML SEMANTICS

Modern, meaningful structure.

1. Why Semantic HTML?
2. Important Semantic Tags:

   * `<header>`
   * `<nav>`
   * `<main>`
   * `<section>`
   * `<article>`
   * `<aside>`
   * `<footer>`
   * `<figure>` + `<figcaption>`
   * `<time>`
3. Accessibility & SEO Advantages
4. Correct Semantic Document Structure

---

# SECTION 6 — HTML FORMS (DETAILED)

A very important topic for interviews & real projects.

### Form Structure

1. `<form>` Element
2. Form Attributes (`action`, `method`, `target`, `enctype`)

### Form Inputs

3. Input Types

   * text
   * number
   * email
   * password
   * date
   * range
   * file
   * checkbox
   * radio
   * hidden
   * submit/reset/button

### Input Attributes

4. Attributes like:

   * placeholder
   * required
   * autofocus
   * minlength, maxlength
   * pattern
   * step
   * disabled
   * readonly

### Form Elements

5. `<textarea>`
6. `<select>` + `<option>`
7. `<datalist>`
8. `<label>`
9. `<fieldset>` & `<legend>`
10. `<output>`
11. Native Form Validation
12. Input Form Attributes (autocomplete, novalidate)

---

# SECTION 7 — HTML LAYOUT & RESPONSIVE DESIGN

Working with structure + RWD.

1. Page Layout Techniques
2. CSS Linking for Layout
3. Using `<div>` vs Semantic Layout Tags
4. HTML Responsive Basics
5. `<meta name="viewport">`
6. Responsive Images (`srcset`, `<picture>`)
7. Responsive Videos
8. Iframes (embedding maps, forms, videos)

---

# SECTION 8 — HTML COMPONENTS (COMMON UI ELEMENTS)

Reusable UI components built through HTML structure.

1. Buttons (`<button>`, `<input type="button">`)
2. Iframes (YouTube, Maps, Websites)
3. Navigation Bars (HTML structure only, CSS later)
4. Dropdowns (HTML + CSS)
5. Tabs structure
6. Cards (HTML layout)
7. Image Gallery Structure
8. Table-based Layouts (legacy understanding)

---

# SECTION 9 — HTML APIS (BROWSER FEATURES)

Powerful browser-native APIs accessible via HTML/JS.

1. HTML Web APIs (overview)
2. Geolocation API
3. Drag and Drop API
4. Web Storage API

   * localStorage
   * sessionStorage
5. Web Workers API
6. SSE (Server-Sent Events)

These require JavaScript to use but are triggered through HTML.

---

# SECTION 10 — HTML GRAPHICS & ADVANCED MEDIA

Specialized UI visuals.

1. Canvas Drawing (JS-powered)
2. SVG Shapes & Paths
3. Embedding vector icons (SVG inline vs file)
4. HTML Filters (CSS + SVG)
5. Masking & Clipping using SVG
6. `<map>` + image maps (old but sometimes needed)

---

# SECTION 11 — HTML STYLE GUIDE / BEST PRACTICES

Professional HTML coding rules.

1. Proper indentation
2. Semantic naming
3. Avoiding inline CSS
4. Using meaningful class names
5. Accessibility guidelines
6. ARIA attributes
7. SEO-friendly structure
8. Avoid deprecated tags
9. HTML Validation
10. Correct file & folder structuring

---

# SECTION 12 — HTML5 FEATURES (MODERN HTML)

HTML5 introduced many new APIs & tags.

1. New semantic tags
2. Audio, Video elements
3. `<canvas>`
4. `<svg>` improvements
5. Web Storage
6. Web Workers
7. Drag & Drop API
8. Offline application concepts
9. New input types (email, date, url, etc.)

---

# SECTION 13 — HTML PROJECTS (PRACTICAL)

This section focuses on real project building.

1. Portfolio Page
2. Landing Page
3. Contact Form
4. Pricing Table
5. Blog Template
6. Product Showcase Page
7. Responsive Navigation Bar
8. Image Gallery
9. Video Player UI
10. Simple Dashboard HTML Structure

---
