#include<iostream> // allows us to use cout (for printing).
using namespace std; // makes life easier (no need to write std::cout).
int main() // starting point of program.
{ 
    cout<<"Hello World";
    return 0; // tells OS program ended successfully.
}
